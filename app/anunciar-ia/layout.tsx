import type { ReactNode } from 'react'

export const metadata = {
  title: 'Anunciar com IA | Plataforma Caminhões',
  description: 'Crie seu anúncio em minutos com ajuda da IA',
}

export default function AnunciarIALayout({ children }: { children: ReactNode }) {
  return <>{children}</>
}
